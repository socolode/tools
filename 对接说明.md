# 对接说明：socolode 设备工具 ↔ corelight-releases 产物仓库

> 本文档写给**另一个沙箱 / 另一位 AI / 协作者**看：说明本仓库（前端 `socolode/tools`）如何对接产物仓库 `socolode/corelight-releases`，以及当前已完成/待办的事项。
> 整个系统的数据流：**固件与应用在 mpy 仓库编译/打包 → 推到产物仓库 `corelight-releases` → 前端页面从这里拉取并展示/下载。**

---

## 1. 两个仓库的分工

| 仓库 | owner/repo | 作用 | 谁写入 |
|---|---|---|---|
| 产物仓库 | `socolode/corelight-releases` | 固件二进制、应用 zip、catalog 索引（**唯一数据源**） | mpy 项目的 `scripts/publish.sh`（只在 mpy 侧执行） |
| 前端展示 | `socolode/tools`（本目录） | 纯静态页面，只**读**产物仓库数据，自己不带任何固件/应用包 | 本仓库直接改 HTML |

> 重要边界：**本仓库不负责发布固件/应用**。新增固件或应用、调整版本，都到 mpy 项目用 `scripts/publish.sh` 完成，产物仓库更新后前端自动拉到。

---

## 2. 产物仓库当前结构（`socolode/corelight-releases`，branch=`main`，2026-09-05 实测）

```
corelight-releases/
├── catalog-firefly-t1.json      # 萤火虫 T1 的设备索引
├── catalog-beetle-s1.json       # 独角仙 S1 的设备索引
├── firmware/
│   ├── firefly-t1/
│   │   ├── v1.4.0/              firmware.bin + bootloader.bin + partition-table.bin
│   │   └── v1.4.1/              同上
│   └── beetle-s1/
│       └── v1.0.0/              firmware.bin + bootloader.bin + partition-table.bin
└── apps/
    ├── firefly-t1/              color-wheel / light-painter / palette / soundlight / wifi-send 各一个 <app>-<ver>.zip
    └── beetle-s1/               color-wheel-1.0.0 / light-painter-1.0.0 / palette-1.1.0 / wifi-send-1.0.0.zip
```

**关键约定：catalog 是「每设备一个文件」**，文件名为 `catalog-<device>.json`。改某台设备只动它自己的文件，绝不互相覆盖。

---

## 3. catalog-<device>.json 结构说明

```jsonc
{
  "schemaVersion": 1,
  "device": "beetle-s1",        // 设备 id，必须与前端 DEV 配置里的 key 一致（firefly-t1 / beetle-s1）
  "updated": "2026-09-05",      // 更新日期（某设备任一项更新都会刷新）
  "firmware": [                 // 固件版本数组，按版本降序（最新在前）
    {
      "version": "1.0.0",
      "date": "2026-09-05",
      "note": "更新说明文案",
      "path": "firmware/beetle-s1/v1.0.0/firmware.bin",
      "size": 2093968
    }
  ],
  "apps": [                     // 应用列表，每个 app_id 只留最高版本
    {
      "app_id": "color-wheel",
      "name": "色相环",          // 中文名
      "name_en": "Hue",         // 英文名
      "version": "1.0.0",
      "min_firmware_version": "1.4.0",   // 依赖的最低固件版本
      "description": "……",
      "icon": "📦",             // 前端一般按 app_id 映射更贴切的图标
      "path": "apps/beetle-s1/color-wheel-1.0.0.zip",
      "size": 384704
    }
  ]
}
```

---

## 4. 前端页面如何引用（firmware-update-v2.html）

接线逻辑都在 `firmware-update-v2.html` 里，已实现：

- 仓库源：`const CATALOG_SOURCE = { owner:'socolode', repo:'corelight-releases', branch:'main' }`
- 每设备独立文件：`const catBase = () => ({ cdn:\`${REPO_BASE}/catalog-${curDev}.json\`, raw:\`${REPO_RAW_BASE}/catalog-${curDev}.json\` })`
  - 优先 jsDelivr CDN（`cdn.jsdelivr.net/gh/…`），失败回退 `raw.githubusercontent.com/…`
  - 请求都带 `t=<时间戳>` 缓存穿透，避免读到旧 catalog
- 设备配置：`const DEV = { "firefly-t1":{…, ready:true}, "beetle-s1":{…, ready:true} }`
  - `ready` 决定该卡是否可点入；**独角仙 S1 已改为 `true`（正式上线）**
  - `loadFirmware()` / `loadApps()` 用 `catBase()` 拉当前 `curDev` 的文件
  - `enterDevice()` 里 `catalog=null` 先清缓存，保证切设备不串数据

---

## 5. 给对接方（另一位 AI）的操作指引

### 本地验证页面
```bash
cd /Users/agui/Workspace/socolode-tools
npx serve .            # 然后浏览器打开 http://localhost:3000/firmware-update-v2.html
```
> 跨端口 fetch 会被 CORS 拦，测试必须走**同源**（`npx serve` 单端口即可）。

### 若只有前端改动
直接改 `firmware-update-v2.html`，然后按第 6 节重新部署。

### 若需要动产物（固件/应用/版本/描述）
**不要**在本仓库改。去 mpy 项目执行（示例，实际以那边为准）：
```bash
cd /Users/agui/Workspace/mpy
CORE_BOARD=beetle-s1 bash scripts/publish.sh all <版本> "<更新文案>"   # 固件+应用一起
```
该脚本会更新产物仓库对应 `catalog-<device>.json` 并推送，前端无需再改即可拉到。

---

## 6. 部署（Cloudflare Pages，纯静态无构建）

方式一（Dashboard）：Cloudflare → Workers & Pages → 创建 Pages → 连接 `socolode/tools` → 框架 None、构建命令留空、构建输出目录 `.`。

方式二（Wrangler）：
```bash
cd /Users/agui/Workspace/socolode-tools
npx wrangler pages deploy . --project-name socolode-tools
```
> 改完 HTML 必须重新部署，线上才能生效。

---

## 7. 踩坑 / 注意事项

- **别再恢复单文件 `catalog.json`**：已拆为 `catalog-<device>.json`，前端只认后者。
- 页面是静态站，引用 CDN（Tailwind / JSZip / Font Awesome），需保证网络可访问。
- 本仓库的固件/应用旧包（旧 `apps/`、旧 `firmware/` 目录）已在 2026-09-04 清理，历史版本可在 git 找回，无需重建。
